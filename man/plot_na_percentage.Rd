% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{plot_na_percentage}
\alias{plot_na_percentage}
\title{Plot the percentage of NA values per column}
\usage{
plot_na_percentage(df, text_labels = TRUE)
}
\arguments{
\item{df}{(data.frame) data frame to inspect}

\item{text_labels}{(logical) \code{TRUE} (default) prints the percentage of \code{NA}
values on every bar}
}
\value{
(ggplot) bar plot with the percentage of \code{NA} values per column
}
\description{
Bar plot with the percentage of \code{NA} values in every column of
a data frame. The bars follow the order of the columns in the data frame.
The percentages are calculated with \code{naniar::miss_var_summary()}.

This function replaces the \code{inspectdf::inspect_na() \%>\% inspectdf::show_plot()}
chart previously used in the QC plots: \code{inspectdf} was archived from CRAN
(2026-04-10), which made the package impossible to install from a clean
library.
}
\examples{
plot_na_percentage(results_named)
}
