% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validations.R
\name{validate_multiple_phases}
\alias{validate_multiple_phases}
\title{validate several phases reported in \code{metadata_phase.txt}}
\usage{
validate_multiple_phases(phase_details, verbose = TRUE)
}
\arguments{
\item{phase_details}{(char) expected output of \code{set_phase}}

\item{verbose}{(logical) \code{TRUE} (default) shows messages}
}
\value{
(char) validation message (only if \code{verbose = TRUE})
}
\description{
Validate a pipe separated combination of phases, dispatching to
the right set of rules:
\itemize{
\item Animal: only the \verb{PASS1A-##|PASS1C-##} pair is supported
(see \code{validate_two_phases()})
\item Human: every phase must be either \code{HUMAN-PRECOVID} or \code{HUMAN-MAIN-TR##},
and no phase can be reported twice. Any number of phases is accepted.
}

Human and animal phases cannot be combined: it throws an error.
}
