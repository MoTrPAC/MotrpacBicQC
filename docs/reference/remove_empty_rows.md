# remove empty rows in data frame

remove empty rows in data frame

## Usage

``` r
remove_empty_rows(df, verbose = TRUE)
```

## Arguments

- df:

  (char) data frame

- verbose:

  (logical) `TRUE` (default) shows messages

## Value

(df) df without empty columns
