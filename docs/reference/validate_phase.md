# Extract PHASE from input folder path

extract ASSAY from input folder path

## Usage

``` r
validate_phase(input_results_folder, return_phase = TRUE)
```

## Arguments

- input_results_folder:

  (char) input_results_folder path

- return_phase:

  (char) return the phase only if `TRUE` (default)

## Value

(vector) PHASE code
