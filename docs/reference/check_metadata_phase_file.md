# check metadata phase file

check the existence of the metadata phase file

## Usage

``` r
check_metadata_phase_file(input_results_folder, verbose)
```

## Arguments

- input_results_folder:

  (char) input path folder

- verbose:

  (logical) `TRUE` (default) shows messages

## Value

(logical) `TRUE` exist, `FALSE` does not
