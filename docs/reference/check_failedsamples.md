# check failed samples file for reported missing vial label ids

check failed samples file for reported missing vial label ids

## Usage

``` r
check_failedsamples(input_results_folder, verbose = TRUE)
```

## Arguments

- input_results_folder:

  (char) input path folder

- verbose:

  (logical) `TRUE` (default) shows messages

## Value

(vector) failed reported ids
