# Get full path to the batch folder

Get the full path to the batch folder

## Usage

``` r
get_full_path2batch(input_results_folder)
```

## Arguments

- input_results_folder:

  (char) path to the PROCESSED/RESULTS folder to check

## Value

(char) Full path to the `BATCH#_YYYYMMDD` folder
